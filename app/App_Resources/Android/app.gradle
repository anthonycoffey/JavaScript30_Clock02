// Add your native dependencies here:

android {
  defaultConfig {  
    generatedDensities = []
    applicationId = "com.nativescript.alarmclock"
  }
  aaptOptions {  
    additionalParameters "--no-version-vectors"  
  }  
} 
