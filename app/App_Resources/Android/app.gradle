// Add your native dependencies here:

android {
  defaultConfig {
    generatedDensities = []
    applicationId = "com.nativescript.clock"
  }
  aaptOptions {
    additionalParameters "--no-version-vectors"
  }
}
